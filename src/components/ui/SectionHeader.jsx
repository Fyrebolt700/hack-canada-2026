export default function SectionHeader({ title }) {
    return (
        <h2 className="text-lg font-semibold text-gray-700 border-b border-gray-200 pb-2 mb-3 mt-6">
            {title}
        </h2>
    );
}